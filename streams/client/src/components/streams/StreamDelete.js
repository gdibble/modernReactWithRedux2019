import React from 'react';
import Modal from '../Modal.js';

const StreamDelete = () => {
  return (
    <div className="">
      Stream Delete
      <Modal />
    </div>
  );
};

export default StreamDelete;
